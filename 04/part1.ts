import { readLines } from "https://deno.land/std@0.167.0/io/mod.ts";

type BingoCard = number[][];
const cards: BingoCard[] = [];

function isWinner(card: BingoCard, calls: number[]) {

}

for await (const line of readLines(Deno.stdin)) {
  
}

console.log(0);